function init() {

    var des  = document.getElementById("description").value;
    var date = document.getElementById("userDate").value;

     alert(des + " will be saved for the following date: " + date);

    document.write(des + ": " + date);
} // end function init