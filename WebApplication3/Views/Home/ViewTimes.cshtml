@using System.Diagnostics.Eventing.Reader
@using System.Web.UI.WebControls
@using WebApplication3.Controllers
@{
    ViewBag.Title = "Milestone List";
}

@model WebApplication3.Models.TimerModel


<div class="row">
    <div class="col-md-4">

        <h1>@ViewBag.Title</h1>

    </div>
</div>


<div>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Description</th>
                <th>Start Date</th>
                <th>End Date</th>
                <th>Days Remaining</th>
                <th>Project Progress</th>
            </tr>
        </thead>


        <tbody>

            @for (int index = 0; index < @Model.TotalEntries; index++)
            {
                var color = " ";

                <tr>
                    <td>@Model.DescriptionList[index]    </td>
                    <td>@Model.StartDates[index]          </td>
                    <td>@Model.EndDates[index]            </td>
                    <td>@Model.ListOfDaysRemaining[index] </td>
                    <td>
                        @Model.PercentList[index]%

                        @if ((@Model.PercentList[index]) > 0 && (@Model.PercentList[index] <= 25))
                        {
                            color = "progress-bar-success";
                        }

                        else if ((@Model.PercentList[index] > 25 && @Model.PercentList[index] <= 50))
                        {
                            color = "progress-bar-info";

                        }
                        else if ((Model.PercentList[index] > 50) && (@Model.PercentList[index] <= 75))
                        {
                            color = "progress-bar-warning";
                        }

                        else if ((Model.PercentList[index] > 75) && (@Model.PercentList[index] <= 100))
                        {
                            color = "progress-bar-danger";
                        }

                        <div class="progress">
                            <div class="progress-bar @color progress-bar-striped" role="progressbar"
                                 aria-valuenow="50" aria-valuemin="0" aria-valuemax="100" style="width: @Model.PercentList[index]%">
                            </div>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- **************************** Headline Div containers ************************
        <div class="container">
          <div class="row">


            <div class="col-md-3">
              <h3>
                Description
              </h3>
            </div>

            <div class="col-md-3">
              <h3>
                Start Date
              </h3>
            </div>

            <div class="col-md-3">
              <h3>
                End Date
              </h3>
            </div>

            <div class="col-md-2">
              <h3>
                Project Completion
              </h3>
            </div>

            <div class="col-md-1">
              <h3>
                Days Remaining
              </h3>
            </div>

          </div>
        </div>


        <!-- **********************  END Headline Div containers ****************


        <div class="container-fluid">
          <div class="row-fluid">
            <!-- ul class="list-unstyled"-->
@*<div class="col-lg-3">
      <ul class="list-group">
        @foreach (var item in @Model.DescriptionList)
        {
          <li class="list-group-item">
            <p>
              <b> @item </b>
            </p>
          </li>
        }
      </ul>
    </div>*@




@*<div class="col-lg-3">
      <ul class="list-group">

        @foreach (var r in @Model.StartDates)
        {
            <li class="list-group-item">
              <p>
                <i> @r </i>
              </p>
            </li>
        }
      </ul>
    </div>*@




@*<div class="col-lg-3">
      <ul class="list-group">

        @foreach (var r in @Model.EndDates)
        {
          <li class="list-group-item">
            <p>
              <i> @r </i>
            </p>
          </li>
        }
      </ul>
    </div>*@


@*<div class="col-lg-2">
      <ul class="list-group">

        @foreach (var x in @Model.PercentList)
        {
          <li class="list-group-item">
            <p>
              @x%
              <div class="progress">
                <div class="progress-bar" role="progressbar" aria-valuenow="10" aria-valuemin="0" aria-valuemax="100" style="width: @x%;">
                </div>
              </div>

            </p>
          </li>
        }
      </ul>
    </div>*@

@*<div class="col-lg-1">
      <ul class="list-group">

        @foreach (var item in @Model.ListOfDaysRemaining)
        {
            <li class="list-group-item">
              <p>
                <b> @item </b>
              </p>
            </li>
        }
      </ul>
    </div>*@


<!-- end of row container
</div>
</div>
  -->



<div class="row">
    <div class="col-md-1">
        <p>
            @Html.ActionLink("Add Time", "Timer", "Home")
        </p>
    </div>

    <div class="col-md-2">
        <p>
            @Html.ActionLink("All Milestones", "ViewAllTimes", "Home")
        </p>

    </div>
</div>
